version: '3.8'
services:
  bot:
    build: .
    container_name: idu_support_bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DB_PATH=/app/db/bot_data.db
    volumes:
      - ./:/app
      - bot_data:/app/db

volumes:
  bot_data:
